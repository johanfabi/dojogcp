apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80 # Puerto del Service
      targetPort: 80 # Puerto del contenedor Nginx
  type: NodePort # Expone el Service en un puerto en cada nodo (el puerto es dinámico, >30000)
