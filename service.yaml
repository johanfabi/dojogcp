apiVersion: v1
kind: Service
metadata:
  name: supertuxkart-game-service
spec:
  selector:
    app: supertuxkart-game
  ports:
    - protocol: TCP
      port: 80 # Puerto del Service (lo que escuchará el LoadBalancer)
      targetPort: 80 # Puerto del contenedor Nginx
  type: LoadBalancer # Exponer como LoadBalancer
